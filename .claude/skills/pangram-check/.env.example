# Copy this file to `.env` (same folder) and put your real key in.
# Get a key from the Pangram dashboard: https://www.pangram.com (API tab).
# The .env file is gitignored, so your key is never committed.
PANGRAM_API_KEY=your_key_here
